# scratch/ — archived investigation specs (NOT part of the suite)

Repro/diagnostic specs from the 2026-07-31 Element Web download/media RCA and
related session investigations. Filenames deliberately do NOT match the suite
glob (`testMatch: 'ew-*.spec.mjs'` in ../playwright.config.mjs), so nothing in
here runs as part of the 231-green suite. Kept as evidence and as starting
points for future repros; expect them to be environment-specific and flaky.
